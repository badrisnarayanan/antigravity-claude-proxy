/**
 * accounts clear command
 *
 * Remove all accounts.
 */

import { getLogger } from "../../utils/logger-new.js";

/**
 * Execute the accounts clear command.
 */
export function accountsClearCommand(): void {
  const logger = getLogger();
  logger.info("accounts clear command - to be implemented");
}
